import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.smartrepublic.app',
  appName: 'Smart Republic Center',
  webDir: 'dist'
};

export default config;
